---
description: Invoke the test-writer agent to add or extend pytest tests for a module.
argument-hint: <module path or description>
---
Invoke the test-writer agent for: $ARGUMENTS

Use the test-writer agent from ~/.claude/agents/test-writer.md.
Work in the current project directory. Target the specified module or the most recently edited Python file if blank.
