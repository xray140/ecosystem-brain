---
description: Run the security-auditor agent on staged changes or provided code. Returns PASS or BLOCK with exact fixes.
argument-hint: [path or blank for staged]
---
Invoke the security-auditor agent on $ARGUMENTS (or staged git changes if blank).

Use the security-auditor agent from ~/.claude/agents/security-auditor.md.
Scope: the current project directory.
